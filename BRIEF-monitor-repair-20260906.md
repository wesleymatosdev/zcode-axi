# Repository-only ZCode monitor repair

Work ONLY inside this repository. Read AGENTS.md and .hermes.md if present and inspect git status before changes. Preserve existing work. Do not request external_directory access; previous worker stopped because its brief was outside repository. Do not access swarm/, installed apps, home config, logs outside repo, credentials, OS permission controls, or any other external paths. No ZCode inference calls, prompt submissions, GUI interaction, permission changes, deployments, pushes, external agent calls or installs. Do not spawn agents. Rust first.

Goal: diagnose and repair existing monitor's full-frame processing failure using repository source and existing evidence; produce tested release binary. Read src/poc.rs, framediff.rs, watch.rs and related capture/OCR/classification source (discover exact names within repo). Historical observations: window matching selected 2560x30 menubar instead of main window. Later full 2560x1440 capture worked, then process stopped between capture and next marker near signature/is_uniform/OCR. A previous assistant called this native abort but that is only an unverified hypothesis. Inspect for actual deterministic faults (dimensions, buffer indexing, loops, overflow, allocations, panic, OCR model readiness), reproduce with tests and existing repo evidence, avoid assuming the crash cause. Fix minimal proven defects and improve stage diagnostics where root cause cannot be reproduced.

Requirements: select actual ZCode main window not menu bar; capture ONLY that window, never full desktop; distinguish unknown/unavailable/error from completed; task-index idle must not prove success. Ensure repeated processing handles realistic full-frame sizes and logs meaningful heartbeat/stage outcomes. No automatic permission approval. Add regression tests first where possible and run actual pipeline against existing repo frames or clearly labeled generated fixtures. Do not represent fixtures as live proof.

Run cargo fmt --check, cargo clippy --all-targets -- -D warnings, cargo test, cargo build --release. Save real output under evidence/monitor-20260906/. Commit scoped changes locally only when tested; preserve unrelated modifications. Write MONITOR-REPAIR-REPORT-20260906.md with demonstrated root cause vs hypotheses, changes, actual command results and evidence paths, commits, exact commands for later bundle launch/live verification. Live GUI capture/60-second monitoring and notification delivery are explicitly PENDING, coordinator-owned gates: do not try reaching outside repository to finish them and do not claim ready until those gates pass. If unable to fix, write evidence-backed blocker report rather than stopping with a plan. No stubs or fabricated results.
